services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sealo
      POSTGRES_USER: sealo
      POSTGRES_PASSWORD: sealo-local
    ports:
      - "5432:5432"
    volumes:
      - sealo-db:/var/lib/postgresql/data

volumes:
  sealo-db:
